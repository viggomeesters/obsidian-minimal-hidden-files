# QA - 2026-06-08

## Environment

- Obsidian: 1.12.7
- Test vault: `/Users/viggomeesters/Dev/obsidian-test-vault`
- Plugin install path: `.obsidian/plugins/minimal-hidden-files`
- Release assets installed: `main.js`, `manifest.json`, `styles.css`

## Build Checks

```bash
npm install
npm run lint
npm run typecheck
npm run build
npm test
```

Result: all passed.

## Go-workflow Route

Target repo:

```text
/Users/viggomeesters/Dev/obsidian-minimal-hidden-files
```

The target repo does not contain `.go-workflow/config.yaml`, so the plan route falls back to standalone execution.

## Manual QA Fixtures

Created in the test vault:

```text
.gitignore
.env
.env.example
.mcp.json
.claude/commands/example.md
.codex/config.md
.obsidian/should-stay-hidden.md
.trash/should-stay-hidden.md
.git/should-stay-hidden
```

## Runtime Checks

After enabling and reloading the plugin, including a rerun after the `Vault.configDir` denylist update:

```json
{
  "enabled": true,
  "files": [
    ".claude/commands/example.md",
    ".codex/config.md",
    ".env",
    ".env.example",
    ".gitignore",
    ".mcp.json"
  ],
  "folders": [
    ".claude",
    ".claude/commands",
    ".codex"
  ],
  "deniedPresent": []
}
```

Denied paths `.obsidian`, `.trash`, and `.git` were not present in the revealed file/folder state.

Native File Explorer DOM contained these dot entries:

```text
.env
.gitignore
.mcp
.claude
.codex
```

Obsidian may strip extensions from file title labels in the DOM, so `.mcp` corresponds to `.mcp.json`.

## Open-flow Checks

Markdown file in an allowed dotfolder:

```json
{
  "type": "markdown",
  "file": ".claude/commands/example.md"
}
```

Unsupported dotfile:

```json
{
  "type": "json-viewer",
  "file": ".mcp.json"
}
```

Both opened via Obsidian's workspace file-open flow in the main workspace.

## Disable / Restore

After disabling the plugin:

```json
{
  "enabled": false,
  "files": [],
  "showUnsupportedFiles": true
}
```

The test vault already had unsupported-file visibility enabled before this plugin loaded, so `true` is the restored previous value.

## Notes

The Obsidian CLI `dev:screenshot` command did not write a screenshot file in this local runtime. DOM inspection and runtime state checks were used as the manual QA evidence.

Release compliance polish added after the first QA run:

- `versions.json` maps plugin version `0.1.0` to minimum Obsidian version `1.8.0`.
- `main.js` is generated locally and uploaded as a release asset, but ignored by Git.
- Settings UI does not add a custom heading for a single section.
- Denylist excludes both `.obsidian` and the active `Vault.configDir`.
